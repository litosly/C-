#include <queue>
using namespace std;


int main(){
	priority_queue<int> heap;
    heap.push(a[i]);
    heap.push(2);

    while(!heap.empty()){
 	   cout<<heap.top();
       heap.pop()
  
    }

}
